_format_version: "3.0"
services:
- name: ai-api
  url: http://mock-api:8002
  routes:
  - name: ai-route
    paths:
    - /ai
    methods:
      - POST    
    plugins:
    - name: guard
      config:
        semantic_api_key: "test-key"